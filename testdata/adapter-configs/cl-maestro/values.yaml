adapterConfig:
  create: true
  files:
    adapter-config.yaml: cl-maestro/adapter-config.yaml
  log:
    level: debug

adapterTaskConfig:
  create: true
  files:
    task-config.yaml: cl-maestro/adapter-task-config.yaml
    manifestwork.yaml: cl-maestro/adapter-task-resource-manifestwork.yaml

broker:
  create: true
  googlepubsub:
    projectId: CHANGE_ME
    subscriptionId: CHANGE_ME
    topic: CHANGE_ME
    deadLetterTopic: CHANGE_ME

image:
  registry: CHANGE_ME
  repository: ci/hyperfleet-adapter
  pullPolicy: Always
  tag: latest

rbac:
   resources:
     - namespaces
     - configmaps
     - configmaps/status
