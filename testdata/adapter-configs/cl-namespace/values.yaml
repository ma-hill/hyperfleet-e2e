adapterConfig:
  create: true
  files:
    adapter-config.yaml: cl-namespace/adapter-config.yaml
  log:
    level: debug

adapterTaskConfig:
  create: true
  files:
    task-config.yaml: cl-namespace/adapter-task-config.yaml

broker:
  create: true
  googlepubsub:
    project_id: CHANGE_ME
    subscription_id: CHANGE_ME
    topic: CHANGE_ME
    dead_letter_topic: CHANGE_ME

image:
  registry: CHANGE_ME
  repository: ci/hyperfleet-adapter
  pullPolicy: Always
  tag: latest

rbac:
   resources:
     - namespaces
     - serviceaccounts
     - configmaps
     - deployments
     - roles
     - rolebindings
     - jobs
     - jobs/status
     - pods
