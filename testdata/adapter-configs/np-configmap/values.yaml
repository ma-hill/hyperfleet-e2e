adapterConfig:
  create: true
  files:
    adapter-config.yaml: np-configmap/adapter-config.yaml
  log:
    level: debug

adapterTaskConfig:
  create: true
  files:
    task-config.yaml: np-configmap/adapter-task-config.yaml
    configmap.yaml: np-configmap/adapter-task-resource-configmap.yaml

broker:
  create: true
  googlepubsub:
    project_id: CHANGE_ME
    subscription_id: CHANGE_ME
    topic: CHANGE_ME
    dead_letter_topic: CHANGE_ME

image:
  registry: CHANGE_ME
  repository: ci/hyperfleet-adapter
  pullPolicy: Always
  tag: latest

rbac:
   resources:
     - configmaps
     - configmaps/status
